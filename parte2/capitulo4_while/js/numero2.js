// 2. Introduce un número entre 50 y 100

var numero = 0; // Valor inicial que permite entrar en el bucle
while ((numero < 50) || (numero > 100)) {
    numero = Number(prompt("Para terminar el bucle, introduce un número entre 50 y 100:"));
}
